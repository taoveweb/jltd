import * as React from 'react';
import { Carousel } from 'antd';

class JltCarousel extends React.Component{
    render() {
        return (
            <Carousel {...this.props} />
        )
    }
}
export default JltCarousel;